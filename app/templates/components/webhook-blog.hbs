<div class="wh-updates">
  <h2>Webhook updates</h2>
  <ul>
  {{#each post in posts}}
    <li>
      <h4>{{post.name}}</h4>
      <p>{{post.short_version}}</p>
      <p class="date">{{format-time post.publish_date}}</p>
    </li>
  {{/each}}
  </ul>
</div>
