<div class="wh-form-builder">
  <div class="wy-grid-for-nav">

    {{partial "form/nav"}}

    <section data-toggle="wy-nav-shift" class="wy-nav-content-wrap">

      <!-- MOBILE NAV, TRIGGLES SIDE NAV ON TOGGLE -->
      <nav class="wy-nav-top">
        <i data-toggle="wy-nav-top" class="fa fa-bars"></i>
        <a href="">Webhook</a>
      </nav>

      <!-- PAGE CONTENT -->
      <div class="wy-nav-content">

        <form class="wy-form-stacked" {{action updateType on="submit"}}>
          <fieldset>
            <h2 class="wh-form-builder-title" data-toggle="tooltip" data-placement="left" title="Click to edit object title.">{{name}}</h2>

            {{#each errors.fields}}
              <span class="wy-form-message">{{this}}</span>
            {{/each}}

            {{#each field in fields}}
              {{formbuilder-widget field=field editingField=editingField doEdit="editField"}}
            {{/each}}

          </fieldset>

          <div class="wh-form-builder-save wy-btn-group">
            <button type="submit" class="btn fa fa-check-circle float-right"> Save form</button>
            <button type="button" class="btn btn-neutral fa fa-times-circle float-right" {{action quitForm}}> Quit</button>

            {{#link-to "wh" class="wh-logo"}}{{/link-to}}
            {{#link-to "wh" class="fa fa-arrow-left"}} Back to the CMS{{/link-to}}
          </div>

        </form>

      </div>

    </section>

  </div>
</div>
