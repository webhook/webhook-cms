<label>
  {{#if view.content.label}}
    {{view.content.label}}
  {{else}}
    {{view.content.controlType.name}}
  {{/if}}
  {{#if buildEnvironment.local}}
    <span class="wh-template-var">{{view.templateVar}}</span>
  {{/#if}}
</label>
