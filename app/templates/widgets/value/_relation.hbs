{{#if control.meta.isSingle}}
  {{view 'relation-value' context=value}}
{{else}}
  {{#view 'relation-values' relationKeys=value}}
    {{each view.slicedKeys itemView='relation-value'}}
    {{#if view.more}}
      and {{view.more}} more
    {{/if}}
  {{/view}}
{{/if}}
