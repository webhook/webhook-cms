{{#each option in meta.data.options}}
  <label class="wy-checkbox">
    {{view 'checkbox-control'
      checked=option.value
      control=control
      option=option
    }}
    {{option.label}}
  </label>
{{/each}}
