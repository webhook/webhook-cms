{{#if success }}

<h2>Almost done</h2>

<p>Great. Point your host records to the following address.</p>

<pre class="hljs">
A-RECORD     @              174.129.25.170
CNAME        www            dns.webhook.com
</pre>
{{else}}
  {{#if session.billing.isTrial }}
    <h2>Upgrade to use custom domains</h2>
    <p>This site is still on a free trial. Only paid sites allow the use of custom domains.</p>
    <a {{bind-attr href=session.billing.url}} class="btn btn-neutral icon icon-credit"> Upgrade at webhook.com</a>
  {{else}}

  {{#if errors}}
    <div class="wy-alert wy-alert-warning">
      <div class="wy-alert-title">Please fix the following errors.</div>
      <ul>{{#each errors}}<li>{{this}}</li>{{/each}}</ul>
    </div>
  {{/if}}

  <form class="wy-form-stacked" {{action "updateDns" on="submit"}}>

    <h1>Domains</h1>

    <p>
      Your site is currently available at secretdemo.webhook.com
    </p>

    <hr/>

    <h2>Use a previously purchased domain</h2>

    <p>
      Don't have a domain? Buy one over at <a href="http://www.namecheap.com/?aff=65093">Namecheap</a> or any domain seller,
      then return here.
    </p>

    <div class="wy-control-group fluid-input">
      <label>Enter your domain name</label>
      <div class="wy-control">
        {{input type="text" value="secretdemo.webhook.com" disabled=true class="wy-input-large" placeholder="example.com"}}
      </div>
    </div>

    <div class="wy-btn-group">
      <button class="btn btn-neutral icon icon-ok-sign btn-disabled" {{bind-attr disabled="isSending"}} > Use this domain</button>
    </div>

  </form>
  {{/if}}
{{/if}}

